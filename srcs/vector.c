# include "../includes/cub3D.h"

t_vec2f	vec2f(float x, float y)
{
	t_vec2f	tmp;

	tmp.x = x;
	tmp.y = y;
	return (tmp);
}

t_vec2i  vec2i(int x, int y)
{
    t_vec2i tmp;

    tmp.x = x;
    tmp.y = y;
    return (tmp);
}